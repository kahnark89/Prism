# CLAUDE.md
This project uses the AGENTS.md open standard as its single source of agent instructions.
**Read `AGENTS.md` in this same directory — it is the canonical instruction file.**
(On systems that support symlinks, run `setup_links.sh` to replace this pointer with a real symlink to AGENTS.md.)
