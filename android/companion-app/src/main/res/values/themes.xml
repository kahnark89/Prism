<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Deliberately minimal: Doc 2.3 puts almost everything on the Compose canvas
      (full-screen color blooms driven by InnerLifeState/CompanionPersona, not XML
      drawables) — this theme exists only to set the activity's window to edge-to-edge
      black before Compose takes over, so Beat 1's "screen goes dark" has nothing
      system-chrome-shaped to contradict it.
    -->
    <style name="Theme.Prism.Companion" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>
